<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= typeof title !=='undefined' ? title : 'PrixClair' %>
    </title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary navbar-custom">
        <div class="container">
            <a class="navbar-brand d-flex align-items-center" href="/">
                <div class="brand-icon me-2">
                    <i class="bi bi-shield-check"></i>
                </div>
                <span>PrixClair</span>
            </a>
            <button class="navbar-toggler border-0" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <i class="bi bi-list"></i>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/"><i class="bi bi-house-door"></i> Accueil</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/prices/consult"><i class="bi bi-search"></i> Consultation</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/prices/map"><i class="bi bi-geo-alt"></i> Carte</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/materials"><i class="bi bi-stack"></i> Matériaux</a>
                    </li>
                    <% if (typeof user !=='undefined' && user) { %>
                        <li class="nav-item">
                            <a class="nav-link" href="/prices/submit"><i class="bi bi-plus-lg"></i> Soumettre</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/alerts"><i class="bi bi-envelope-paper"></i> Alertes</a>
                        </li>
                        <li class="nav-item dropdown">
                            <a class="nav-link position-relative d-flex align-items-center" href="#"
                                id="notificationsDropdown" data-bs-toggle="dropdown">
                                <i class="bi bi-bell"></i>
                                <span class="d-lg-none ms-2">Notifications</span>
                                <span class="badge rounded-pill bg-danger" id="notificationBadge"
                                    style="display: none; padding: 0.35em 0.65em;">0</span>
                            </a>
                            <ul class="dropdown-menu dropdown-menu-end shadow-lg border-0 mt-2" id="notificationsList"
                                style="width: 350px; border-radius: 12px; overflow: hidden;">
                                <li>
                                    <div
                                        class="dropdown-header py-3 bg-light d-flex justify-content-between align-items-center">
                                        <span class="h6 mb-0 fw-bold">Notifications</span>
                                        <button class="btn btn-sm btn-link text-decoration-none p-0"
                                            id="markAllRead">Tout lire</button>
                                    </div>
                                </li>
                                <div id="notificationsLoading" class="p-3 text-center">
                                    <div class="spinner-border spinner-border-sm text-primary" role="status"></div>
                                </div>
                                <li>
                                    <hr class="dropdown-divider m-0">
                                </li>
                                <li>
                                    <a class="dropdown-item text-center py-2 bg-light small fw-bold"
                                        href="/notifications">
                                        Voir tout l'historique
                                    </a>
                                </li>
                            </ul>
                        </li>
                        <% } %>
                </ul>
                <ul class="navbar-nav align-items-center">
                    <% if (typeof user !=='undefined' && user) { %>
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle d-flex align-items-center user-profile-link" href="#"
                                id="navbarDropdown" role="button" data-bs-toggle="dropdown">
                                <div class="user-avatar me-2">
                                    <i class="bi bi-person-circle"></i>
                                </div>
                                <span class="fw-bold">
                                    <%= user.nom %>
                                </span>
                            </a>
                            <ul class="dropdown-menu dropdown-menu-end shadow-lg border-0 mt-2"
                                style="border-radius: 12px;">
                                <li><a class="dropdown-item py-2" href="/prices/my-submissions">
                                        <i class="bi bi-folder2-open me-2"></i> Mes soumissions</a></li>
                                <% if (user.typeCompte==='commercant' ) { %>
                                    <li><a class="dropdown-item py-2" href="/materials/add">
                                            <i class="bi bi-plus-square me-2"></i> Ajouter matériau</a></li>
                                    <% } %>
                                        <li>
                                            <hr class="dropdown-divider">
                                        </li>
                                        <li><a class="dropdown-item py-2 text-danger" href="/auth/logout">
                                                <i class="bi bi-box-arrow-right me-2"></i> Déconnexion</a></li>
                            </ul>
                        </li>
                        <% } else { %>
                            <li class="nav-item">
                                <a class="nav-link fw-bold" href="/auth/login"><i class="bi bi-box-arrow-in-right"></i>
                                    Connexion</a>
                            </li>
                            <li class="nav-item ms-lg-2">
                                <a class="btn btn-light text-primary fw-bold px-4 rounded-pill" href="/auth/register">
                                    S'inscrire
                                </a>
                            </li>
                            <% } %>
                </ul>
            </div>
        </div>
    </nav>

    <script>
        // Set active link based on current path
        document.addEventListener('DOMContentLoaded', function () {
            const currentPath = window.location.pathname;
            document.querySelectorAll('.navbar-nav .nav-link').forEach(link => {
                const href = link.getAttribute('href');
                if (href === currentPath || (currentPath.startsWith(href) && href !== '/')) {
                    link.classList.add('active');
                }
            });
        });
    </script>

    <div class="container mt-4">
        <% if (typeof success !=='undefined' && success.length> 0) { %>
            <% success.forEach(msg=> { %>
                <div class="alert alert-success alert-dismissible fade show" role="alert">
                    <%= msg %>
                        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                </div>
                <% }); %>
                    <% } %>

                        <% if (typeof error !=='undefined' && error.length> 0) { %>
                            <% error.forEach(msg=> { %>
                                <div class="alert alert-danger alert-dismissible fade show" role="alert">
                                    <%= msg %>
                                        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                                </div>
                                <% }); %>
                                    <% } %>